clc

% Carregar imagens e identificadores
imagens = if(carrega_imagens());
K = identificadores();

% soma e desvio padrão dos pixels das imagens
X_std_sum = std_sum_imagem(imagens);

% Soma e desvio padrão após aplicado filtro de gabor
X_gabor = filtro_gabor(imagens,4,90);

[F,C,POST] = lda(X_gabor,K);
resp = sum(C==K)/size(C,1);
fprintf('Acurácia de %f \n',resp);

[F,C,POST] = lda(X_std_sum,K);
resp = sum(C==K)/size(C,1);
fprintf('Acurácia de %f \n',resp);





